#ifndef NANARO_GRID_GENERATE_HEAD_HPP
#define NANARO_GRID_GENERATE_HEAD_HPP
#include <cstddef>

namespace Grid {
  void generate_grid(std::size_t count); //Count is the number of rooms other than spawn and exit
}

#endif //NANARO_GRID_GENERATE_HEAD_HPP
